// Workspace / organization switcher — Enterprise Edition.
// Community-edition stub: community deployments are single-organization, so there
// is nothing to switch between. The full switcher ships with OpsCat Cloud.
// See docs/OPEN-CORE.md for where the open-core line is drawn.
export default function OrgSwitcher(_props: { edition: string | null }) {
  return null;
}
