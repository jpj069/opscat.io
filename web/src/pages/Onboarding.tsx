// Guided first-run onboarding — Enterprise Edition.
// Community-edition stub: community deployments are single-organization and never
// trigger onboarding (App.tsx gates it to the cloud edition). The full setup flow
// ships with OpsCat Cloud. See docs/OPEN-CORE.md.
export default function Onboarding() {
  return null;
}
